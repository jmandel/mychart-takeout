{
  "name": "mychart-export",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build:web": "bun run apps/web-build/build.ts",
    "gen:skill": "bun tools/gen-export-skill.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "typescript": "^5.7.3",
    "playwright-core": "^1.50.0"
  }
}
